# Copy to .env and fill in ONLY if you want to test on Sepolia.
# Hardhat runs fine with no .env (in-process node / localhost). No real funds needed.
SEPOLIA_RPC_URL=https://sepolia.infura.io/v3/YOUR_PROJECT_ID
PRIVATE_KEY=YOUR_TEST_WALLET_PRIVATE_KEY